{
  "name": "bot-deflector",
  "version": "1.3.0",
  "description": "A Manifest V3 browser extension for Reddit that detects and stealthily deflects bot accounts and repost farms.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.js",
    "build:userscript": "node scripts/build-userscript.js",
    "build:all": "npm run build && npm run build:userscript",
    "package": "npm run build && npm run build:userscript && node scripts/package.js",
    "dev": "node scripts/build.js",
    "test": "node --import tsx --test src/**/*.test.ts",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "keywords": [
    "reddit",
    "bot-detector",
    "browser-extension",
    "manifest-v3",
    "spam-filter"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
